<table>
  <thead>
    <tr>
      <th>Fecha compra</th>
      <th>Evento</th>
      <th>Locación</th>
      <th>Cantidad de entradas</th>
      <%if current_user.type.eql?("Admin")%>
        <th>Cliente</th>
      <%end%>
    </tr>
  </thead>
  <tbody>
    <%@all_reservations.each do |reservation|%>
      <tr>
        <td><%=reservation.created_at.localtime.strftime("%F %T")%></td>
        <td><%=reservation.location_type.sale_event.title%></td>
        <td><%=reservation.location_type.name%></td>
        <td><%=reservation.quantity%></td>
        <%if current_user.type.eql?("Admin")%>
          <td><%=reservation.client.email%></td>
        <%end%>
      </tr>
    <%end%>
  </tbody>
</table>
